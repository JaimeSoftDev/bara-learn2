# Production builds must NOT bake in a dev API host: the SPA and the
# Laravel API are served from the same origin (see docs/deploy-hostinger.md),
# so this stays empty regardless of what frontend/.env sets for local dev.
# Vite loads .env.production with higher priority than .env for `vite build`.
VITE_API_URL=
